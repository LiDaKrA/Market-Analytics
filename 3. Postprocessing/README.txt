Postprocessing
---------------------------------------
1. Images
--------------------------------------- 
In order to reduce the size of images, we want every image excatly once instead of multiple times
After this step, we can delete the unpacked raw data (webpages) and keep the image repository and the database instead
To do this, we have to determine if the images have to be converted from base64 or if they are already there (as jpg or png)
For the example marketplaces:
Silkroad2: 	Select and Convert
Agora:		Select
Dogeroad:	Select

To do these steps cd into the directory and call

2. Dollar BTC Euro conversion
---------------------------------------

3. Whatelse (holding prices)
---------------------------------------